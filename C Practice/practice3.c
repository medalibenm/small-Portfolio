#include <stdio.h>

// Function prototypes
void print_month(int month, int year);
int get_days_in_month(int month, int year);
void print_calendar(int year);

// Main function
int main() {
    int year;
    printf("Enter the year for the calendar: ");
    scanf("%d", &year);

    print_calendar(year);

    return 0;
}

// Function to print a month's calendar
void print_month(int month, int year) {
    // Your code here
}

// Function to get the number of days in a month
int get_days_in_month(int month, int year) {
    // Your code here
}

// Function to print the calendar for a year
void print_calendar(int year) {
    // Your code here
}
